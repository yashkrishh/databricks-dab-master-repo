# This is a Databricks asset bundle definition for dapp.
bundle:
  name: dapp
  uuid: 2fb134f3-f8ce-4bf4-b969-d03820bf7fa3

include:
  - resources/*.yml

# Variable declarations. Kept safely intact!
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://adb-7405608553806739.19.azuredatabricks.net
      # 🛡️ FIXED: root_path is now correctly indented under the workspace block
      root_path: /Workspace/Users/yashkrishh1991@gmail.com/.bundle/${bundle.name}/${target.name}
    variables:
      catalog: dbx_workspace_dev
      schema: ${workspace.current_user.short_name}

  prod:
    mode: production
    workspace:
      host: https://adb-prod-workspace-production-url.net
      # 🛡️ FIXED: Indented correctly and changed legacy variable suffix to ${target.name}
      root_path: /Shared/deployments/production_pipelines/${bundle.name}/${target.name}
    variables:
      catalog: dbx_workspace_dev
      schema: prod
    permissions:
      - user_name: yashkrishh1991@gmail.com
        level: CAN_MANAGE